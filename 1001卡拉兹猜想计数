#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main()
{
	int input = 3;
	int count = 0;
	scanf("%d", &input);
	while (input != 1)
	{
		if (input % 2 == 0)
		{
			input = input / 2;
			count++;
		}
		else
		{
			input = (input * 3 + 1) / 2;
			count++;
		}
	}
	printf("%d", count);
}
