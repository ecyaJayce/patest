//1002写出这个数
#include<stdio.h>
#include<string.h>
int main()
{
	int i=0;
	int sum=0;
	char arr[101]={'1','2','3','4'};
	for(i=0;i<strlen(arr);i++)
	{
		sum=sum+arr[i]-'0';
	}
	//已经得到数字相加的结果，但是还未转化为拼音
	return 0;
}
