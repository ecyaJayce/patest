#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int main()
{
	int i = 0;
	int input = 0;
	int width = 0;
	int arr[100] = {};
	scanf("%d %d", &input, &width);
	width = width % input;
	for (i = 0; i < input; i++)
	{
		scanf("%d", &arr[i]);
	}
	if (input -width<input)
	{
		printf("%d", arr[input - width]);
		for (i = input - width + 1; i < input; i++)
		{
			printf(" %d", arr[i]);
		}
		for (i = 0; i < input - width; i++)
		{
			printf(" %d", arr[i]);
		}
	}
	else
	{
		printf("%d", arr[0]);
		for (i = 1; i < input; i++)
		{
			printf(" %d", arr[i]);
		}
	}
	return 0;
}
