/*令 Pi表示第 i 个素数。现任给两个正整数 M≤N≤10^4，请输出PM到PN的所有素数*/
#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<math.h>
int main()
{
	int m, n, l;
	int k = 0;
	int i = 0;
	int j = 0;
	int arr[10000] = {};
	scanf("%d%d", &m, &n);
	l = 1;
	if (m == 1)
	{
		arr[k] = 2;
		k++;
	}
	for (i = 3;1; i +=2)
	{
		for (j = 3; j < sqrt(i); j += 2)
		{
			if (i % j == 0)
				break;
		}
		if (j>sqrt(i))
		{
			l++;
			if (l > n)    //此if不能与下面的if顺序颠倒：为了保证m=n也能运行
				break;
			if (l >= m)
			{
				arr[k] = i;
				k++;
			}
		}
	}
	for (i = 0; i < k-1; i++)
	{
		if (i % 10 != 9)
			printf("%d ", arr[i]);
		else
			printf("%d\n", arr[i]);
	}
	printf("%d", arr[k-1]);
	return 0;
}
